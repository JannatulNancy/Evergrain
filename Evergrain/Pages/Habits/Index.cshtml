@page
@model Evergrain.Pages.Models.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Habit[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Habit[0].HabitNo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Habit[0].HabitName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Habit[0].StartingDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Habit[0].EndingDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Habit) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HabitNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HabitName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartingDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndingDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
