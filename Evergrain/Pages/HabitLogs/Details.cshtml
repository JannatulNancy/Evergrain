@page
@model Evergrain.Pages.HabitLogs.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>habitLog</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.habitLog.Date)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.habitLog.Date)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.habitLog.IsCompleted)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.habitLog.IsCompleted)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.habitLog.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
